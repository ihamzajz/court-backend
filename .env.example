NODE_ENV=production
PORT=5000

DB_HOST=localhost
DB_USER=u750098024_court_user
DB_PASSWORD=
DB_NAME=u750098024_court_db

# Use a long random secret in production. Minimum 32 characters.
JWT_SECRET=replace-with-a-long-random-secret-at-least-32-characters

# Comma-separated allowed origins in production.
# Example:
# CORS_ORIGIN=https://app.example.com,https://admin.example.com
CORS_ORIGIN=https://nngt.com.pk,https://www.nngt.com.pk

# Use a persistent disk path in production if your host does not keep local files.
# UPLOADS_DIR=/var/www/bookflow-uploads

# Optional email configuration for forgot-password OTP.
# MAIL_USER=your-gmail-address
# MAIL_APP_PASSWORD=your-gmail-app-password
# MAIL_FROM_EMAIL=no-reply@example.com
# MAIL_FROM_NAME=BookFlow
